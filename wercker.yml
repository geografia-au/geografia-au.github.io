box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: geografia-au/geografia-au.github.io
        path: public
        domain: geografia.com.au
