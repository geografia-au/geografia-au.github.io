box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.17"
        basedir: "source"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - leipert/git-push:
        token: $GIT_TOKEN
        repo: geografia-au/geografia-au.github.io
        domain: geografia.com.au
        basedir: public
